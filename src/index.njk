---
layout: layouts/home.njk
---

<div class="">
  {# Show my ~musings~ #}
  <h1 class="page-header">Musings</h1>
  {% set data = collections.sortedMusings | head(4) %}
  {% set counter = collections.sortedMusings | length %}
  {% include "musingslist.njk" %}

  {%- if counter > 4 %}
  <p class="pt-4">More musings can be found in <a class="underline" href="{{ '/musings' | url }}">the archive</a>.</p>
  {%- endif %}

  {# List out my recs #}
  {% include "recs.njk" %}

  <div class="hidden">
    An occasionally (seldom) updated site with some musings and links to what I find interesting.
  </div>
</div>
