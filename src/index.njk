---
layout: layouts/home.njk
---

<div>
    {# I guess tell the people who I am #}
    <p class="mb-6">
       Software engineer, professional amateur, average cyclist, wannabe writer, and reader. I create things, get obsessed with a topic for a month before switching, and periodically update this site.
    </p>

    {% set maxCount = 5 %}
    {# Show my ~notes~ #}
    <h1 class="page-header mt-12">Notes</h1>
    {% set data = collections.sortedNotes | head(maxCount) %}
    {% set counter = collections.sortedNotes | length %}
    {% include "post-list.njk" %}
    {%- if counter > maxCount %}
        <p class="pt-4">
            More notes can be found in <a class="underline" href="{{ '/posts/' | url }}">the archive</a>.
        </p>
    {%- endif %}

    {# Show my ~essays~ #}
    <h1 class="page-header mt-12">Essays</h1>
    {% set data = collections.sortedEssays | head(maxCount) %}
    {% set counter = collections.sortedEssays | length %}
    {% include "post-list.njk" %}
    {%- if counter > maxCount %}
        <p class="pt-4">
            More essays can be found in <a class="underline" href="{{ '/posts/' | url }}">the archive</a>.
        </p>
    {%- endif %}

    {# List out my recs #}
    {% include "recs.njk" %}
    <div class="hidden">An occasionally (seldom) updated site with some musings and links to what I find interesting.</div>
</div>
